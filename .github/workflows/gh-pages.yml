name: Build and deploy to GitHub Pages

on:
  push:
    branches: [main]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout 🛎️
        with:
          repository: nikonenkoincodeit/test-deploy

      - name: DDDDD
        run: git checkout -b new-branch
